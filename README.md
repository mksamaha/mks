
apt update -y && apt upgrade -y
apt install git -y

git clone https://github.com/mksamaha/mks.git

cd mks

chmod +x install.sh

./install.sh


-----------

apt install git -y

git clone https://github.com/mksamaha/mks.git

cd mks

chmod +x install2.sh

./install2.sh
